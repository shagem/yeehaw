{
  "name": "yeehaw",
  "version": "1.0.0",
  "description": "",
  "main": "index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "tailwind": "npx tailwindcss -i ./css/input.css -o ./css/output.css --minify",
    "esbuild": "esbuild ./js/input.js --bundle --outfile=./js/output.js",
    "build": "npm run tailwind && npm run esbuild"
  },
  "author": "Sean Hageman",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "0.23.1",
    "tailwindcss": "^3.4.11"
  }
}
